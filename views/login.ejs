<%- include('partials/header') %>

<div class="form">
	    <% if(locals.error) { %>
	        <div class="alert alert-danger"><%= error %></div>
	    <% } %>

	    <% if(locals.success) { %>
	        <div class="alert alert-success"><%= success %></div>
	    <% } %>

	<form method="post">
				<p>Especificación de requisitos</p>
	    	<div class="thumbnail"><img src="imagelogin.png"></div>
	        
	        <div class="input">
		        <label  for="email">Usuario</label>
		        <input type="email" name="email" id="email" value="<%= locals.email || '' %>" class="form-control" />
	        </div>
	    	<br>
	        <div class="input">
	            <label for="password">Password</label></center>
	            <input type="password" name="password" id="password" class="form-control" />
	        </div>
	    	<br>
	      	<div class="button">
	           	<button type="submit" class="button btn-primary">Iniciar sesión</button>
	        </div>
	        <br>
	             <p class="message"> No te has registrado todavía? <a href="/register">Registrate </a></p>
	</form>
</div>
<%- include('partials/footer') %>